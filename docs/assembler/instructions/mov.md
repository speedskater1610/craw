# The mov instruction
copies data from source to destination, this can be a register, memory location, or an immediate value   
`reg`, `reg` / `reg`, `imm` / `reg`, `[reg]` / `[reg]`, `reg` / with displacement
