# How to build

1. First clone the repo 
```shell
git clone https://github.com/speedskater1610/craw
```

2. Then build the project using the makefile (Works on both windows and linux)
```shell
make -f Makefile.mk all
```

3. Then make the exacutable global
*this depends on the system so reference the documentation for your operating system*