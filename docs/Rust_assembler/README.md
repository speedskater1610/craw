# Rust assembler. 

An assembler written in rust, this is a more complete and "safer" version of the old C++ assembler, that is still a part of this project, because I spent time on it and already have thrown away a working assembler. 

## Reasons for a new assembler.
The old assembler would only produce a single ELF32 binary, this is fine for simple programs, that need debugging, but 