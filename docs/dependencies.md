# All of the dependencies for the compiler currently 

*Note depending on the version you use not all of these might be mandatory.*


- `readline`
    - Choose the following to install
        - `sudo apt-get install libreadline-dev`
        - `yum install readline-devel`
        - `sudo pacman -S readline`
